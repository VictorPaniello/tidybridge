# Working preferences

- **Avoid claude-in-chrome browser automation when it can be avoided.**
  For dashboard-only tasks (Railway, Vercel, Cloudflare, GitHub, etc.),
  prefer giving Victor clear step-by-step instructions to do it himself
  over driving the browser directly. Only use claude-in-chrome when
  there's genuinely no other way to get it done (or Victor explicitly
  asks for it to be driven directly).
